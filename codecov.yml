coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

ignore:
  - "src-tauri/gen/**"
  - "scripts/**"
  - "src/**/__tests__/**"

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
