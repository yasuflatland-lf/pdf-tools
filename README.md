# PDF Tools

PDF Tools is a desktop PDF composition tool for combining PDFs and images into a single PDF, built with Tauri 2, React, and Rust, with PDF rendering powered by PDFium.

## Development

```sh
mise install
pnpm install
pnpm tauri dev
pnpm build
cargo build --workspace
cargo nextest run --workspace
```
