# oxfmt reads .gitignore and .prettierignore by default.
# src/bindings/ is regenerated by ts-rs on every `cargo test`, so formatting it
# would just be undone by the next Rust test run.
src/bindings/
